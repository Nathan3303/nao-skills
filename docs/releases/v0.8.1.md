# v0.8.1 — 测试设计补「反事实守卫」（上游化 nao-todo 沉淀）

- 状态：已发布 · 更新：2026-09-25 · Owner：PM

## 一句话

把 nao-todo 里 qa 沉淀的「**反事实守卫**」方法原文收进上游技能，避免 `nao-skill update`（源优先）把项目沉淀冲掉。

## 亮点 / 变更

- `.agents/skills/test-design.md` 新增一节 **「反事实守卫（把静态推断升级为实测结论）」**：两段式探针（S1 实际链路 / S2 反事实分支）、守卫化与红绿分列、结论绑定被测外部系统版本
  - 来源：nao-todo `T187` qa 沉淀；本次**原文上游化**（措辞不改）
- 新增 `docs/reports/2026-09-25-agents-sync-inventory.md`：nao-todo `.agents/` 相对上游的 3-way 盘点报告（本单同期产出）

## 行为（用户可见）

- **无行为变化**：纯知识资产补充与盘点报告，不改任何命令、脚本、角色卡语义与分发文件。
- 版本号按 **patch** 处理（`0.8.0 → 0.8.1`）：既有技能文档补内容 + 新增报告，无新增能力。

## 升级注意

`npm i -g @nathan33/nao-skill@0.8.1`，或项目内 `nao-skill update`。

- 角色卡版本不变：`pm v19` / `rd-fe v10` / `rd-be v10` / `qa v11` / `arch v10`。
- 对 nao-todo：本版是「先上游化、再 update」链路的第一半；上游合并后再执行 `nao-skill update`，即可在保住本地沉淀的前提下补齐 v0.8.0 的 `qq-notify` 资产。

## 回滚

`git revert <merge commit>`；纯文档变更，无数据迁移。
