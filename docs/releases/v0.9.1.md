# v0.9.1 — PM 卡瘦身（内容外迁到按需技能）

- 状态：已发布 · 更新：2026-09-25 · Owner：PM

## 一句话

PM 角色卡从 **198 行瘦到 129 行**（常驻成本 −35%），把流程细节搬到两个按需技能文件——**无行为变化，仅重组**。

## 亮点 / 变更

- **新增 `.agents/skills/pm-routing.md`**（31 行）：路由口诀（业务行为 ⇒ `rd-fe`/`rd-be`；构建/验/发/治理 ⇒ `rd-infra`）· 工程基座类与**兜底纪律**（显式声明 + 选最接近专精角色 + 临时附加清单 + 台账登记）· **技术调研边界**（可自做 / 必须转角色 6 条 / 转发对象 / 判定口径）
- **新增 `.agents/skills/pm-operations.md`**（92 行）：多会话调度细则 · 开工确认卡 · **PM 会话生命周期**（重开三触发点）· **AGENTS.md 项目上下文治理**（内容范围/合并规则/更新时机/自查）· 交付归档
- **PM 卡 v20 → v21（198 → 129 行）**：保留**身份 / 边界 / 红线 / 硬交接**（§一 核心约束、§十 硬性红线、§十三 Issue/PR/发布），其余压缩为**硬纪律摘要 + 指针**
- **闸门保留完整原文于卡内**（不靠翻技能文件才想起）：开工确认闸门 · 环境自检 · 架构评审闸门 · 忙闲闸门 · 终态回执闸门 · 验收闭环 · **合并闸门（含「恰好 1 条提交/无 `wip()`/用户可读」验收核对）**
- **引用同步**：`common/intercom-protocol.md`、`checklists/pm.md`、`skills/research.md`、`docs/ARCHITECTURE.md`、`bin/nao-skill.js` 的相关指向改为「PM 卡小节 + 技能文件」

## 行为（用户可见）

- **无行为变化，仅重组**：所有规则**逐条保留**，只是从常驻卡移到按需技能与清单；PM 卡小节号 `§一`–`§十四` **保持稳定**（外部引用不失效）。
- `nao-fleet.sh check` → `roles=6`，`.agents/**` 全 LF、无 Tab。

## 限制

- 相关细则多一次**按需读取**（skills 仅在对应操作时加载，常态零成本）；
- 常驻卡的「硬纪律摘要」不含完整细则，执行细则类操作时**必须**按指针读取技能文件。

## 升级注意

`npm i -g @nathan33/nao-skill@0.9.1`，或项目内 `nao-skill update`。

- 角色卡版本：**`pm v21`** / `rd-infra v1` / `rd-fe v10` / `rd-be v10` / `qa v11` / `arch v10`。
- **已拉起的 PM 会话需重开**（`nao-fleet.sh ensure --force pm`）才会加载瘦身后的卡与技能指引。
- 无数据迁移。

## 回滚

`git revert <merge commit>`；纯文档重组，回滚后 PM 卡回到 198 行版本。
