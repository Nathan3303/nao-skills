# 任务状态（PM 维护，运行时事实）

> 每次派发 / 回执 / 验收 / 抢占后更新本文件；PM 会话重开（`ensure --force`）后**先读本文件重建状态**再继续调度。
> 归档后的历史见 `docs/prds/` 与 `docs/adr/`；本文件只记**运行时**任务状态，保持精简。

## PM 接续快照（会话重开后**先读本区**）

> 本区是 PM 的**悬空状态**（任务状态见下方五栏）。每次重开前更新；重开后逐行回读确认（当前阶段 · 未决决策 · 口头约束）。

- 当前阶段：<澄清 / PRD / 派发中 / 验收中 / 已归档待重开>
- 当前 PRD：`docs/prds/YYYY-MM-DD-<主题>.md`（状态：<草稿 / 待用户确认开工 / 已开工 / 已归档>）
- 未决决策点：<等谁拍板 → 事项；无则写「无」>
- 待用户回答：<Q1 → 问题；无则写「无」>
- 未派发队列：见下方「待派发队列」（共 N 项）
- 下次唤醒条件：<用户答复 Q1 / worker 回执 T1 / 定时>
- 口头约束已落盘：<`AGENTS.md` §… / PRD「变更治理」；无则写「无」>
- 会话体检：contextTokens=<n>（窗口 <x>%）· 压缩次数=<n> · cacheRead=<x>%（每次归档后更新）

## 待派发队列

| 任务编号 | 目标会话 | 角色 | 概要 | 排队原因 |
| :--- | :--- | :--- | :--- | :--- |
| T3 | rd-be | 后端 | 实现 X 接口 | 目标忙碌 |

## 进行中

| 任务编号 | 目标会话 | 角色 | 概要 | 派发时间 | 对应 AC |
| :--- | :--- | :--- | :--- | :--- | :--- |
| T2 | rd-fe | 前端 | 登录页 + 校验 | 2026-09-21 14:00 | AC1/AC2 |

## 已回执待验收

| 任务编号 | 回执摘要（≤150字） | 详情路径 | 待办 |
| :--- | :--- | :--- | :--- |
| T1 | 创建 todo 链路完成 | docs/reports/T1.md | 核 AC 五覆盖 |

## 挂起（被抢占 / 降级）

| 任务编号 | 挂起原因 | 恢复方式 |
| :--- | :--- | :--- |
| — | — | — |

## 已验收 / 已归档

| 任务编号 | 结论 | 归档路径 |
| :--- | :--- | :--- |
| — | — | `docs/prds/` |
